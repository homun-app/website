<div
	class="memory-continuity"
	data-illustration="memory-continuity"
	role="img"
	aria-labelledby="memory-continuity-title memory-continuity-description"
>
	<span id="memory-continuity-title" class="sr-only">Homun memory continuity</span>
	<span id="memory-continuity-description" class="sr-only">
		A decision captured in Project Atlas becomes structured SQLite recall, connected graph context,
		and a readable Markdown wiki, then returns with its source in a new conversation.
	</span>

	<article class="memory-stage capture-stage" data-memory-stage="capture">
		<header>
			<span>Yesterday · Project Atlas</span>
			<span class="stage-state"><i aria-hidden="true"></i>Captured</span>
		</header>
		<div class="stage-body">
			<div class="message-meta"><strong>Fabio</strong><time>16:42</time></div>
			<p class="message-bubble">
				Keep Ollama local for private documents. Use cloud only for presentation rendering.
			</p>
			<div class="extract-block">
				<p>What matters</p>
				<div class="knowledge-tags" aria-label="Extracted knowledge">
					<span>Decision</span><span>Privacy</span><span>Model routing</span>
				</div>
			</div>
		</div>
		<span class="story-flow flow-out" aria-hidden="true"></span>
	</article>

	<article class="memory-stage faces-stage" data-memory-stage="three-faces">
		<header>
			<span>One memory · three synchronized faces</span>
			<span class="stage-state"><i aria-hidden="true"></i>Connected</span>
		</header>
		<div class="face-stack">
			<section class="memory-face sql-face" data-memory-face="sql">
				<div class="face-heading"><strong>01 · Recall</strong><span>SQLite + FTS</span></div>
				<ul>
					<li><b>decision</b><span>16:42</span></li>
					<li><b>local model</b><span>private</span></li>
					<li><b>cloud render</b><span>opt-in</span></li>
				</ul>
			</section>

			<section class="memory-face graph-face" data-memory-face="graph">
				<div class="face-heading"><strong>02 · Connections</strong><span>Graph + why</span></div>
				<svg viewBox="0 0 260 108" aria-hidden="true">
					<path d="M130 54 48 24M130 54 218 22M130 54 65 91M130 54 210 90"></path>
					<circle class="graph-centre" cx="130" cy="54" r="8"></circle>
					<circle cx="48" cy="24" r="5"></circle>
					<circle class="graph-blue" cx="218" cy="22" r="5"></circle>
					<circle class="graph-pink" cx="65" cy="91" r="5"></circle>
					<circle class="graph-gold" cx="210" cy="90" r="5"></circle>
					<text x="37" y="13">Project</text>
					<text x="198" y="11">Privacy</text>
					<text x="43" y="106">Model</text>
					<text x="187" y="106">Why</text>
				</svg>
			</section>

			<section class="memory-face wiki-face" data-memory-face="wiki">
				<div class="face-heading"><strong>03 · Understanding</strong><span>Markdown wiki</span></div>
				<p class="wiki-path">project-atlas / decisions.md</p>
				<h3>Why we chose local models</h3>
				<p>Private documents stay on-device. Cloud rendering remains available only when explicitly enabled.</p>
				<div class="wiki-lines" aria-hidden="true"><i></i><i></i><i></i></div>
			</section>
		</div>
	</article>

	<article class="memory-stage recall-stage" data-memory-stage="recall">
		<header>
			<span>Today · New chat</span>
			<span class="stage-state"><i aria-hidden="true"></i>Recalled</span>
		</header>
		<div class="stage-body">
			<p class="no-context">No context pasted</p>
			<h3>I remember the boundary you chose.</h3>
			<p class="answer-copy">Private documents stay local. Cloud presentation rendering remains opt-in.</p>
			<div class="memory-source"><i aria-hidden="true"></i><span>Source · Project Atlas<br />Decision captured yesterday</span></div>
			<ul class="memory-actions" aria-label="Memory controls">
				<li data-memory-action="inspect">Inspect</li>
				<li data-memory-action="correct">Correct</li>
				<li data-memory-action="forget">Forget</li>
			</ul>
		</div>
		<span class="story-flow flow-in" aria-hidden="true"></span>
	</article>

	<span class="sr-only">
		Recall — SQLite + FTS. Connections — Graph + why. Understanding — Markdown wiki.
	</span>
</div>

<style>
	.memory-continuity {
		position: relative;
		display: grid;
		grid-template-columns: minmax(0, 1.02fr) minmax(28rem, 1.48fr) minmax(0, 1fr);
		gap: 0.85rem;
		align-items: stretch;
	}

	.memory-stage {
		position: relative;
		min-width: 0;
		min-height: 22rem;
		overflow: hidden;
		border: 1px solid var(--color-line);
		border-radius: 1.2rem;
		background:
			radial-gradient(circle at 50% 42%, rgba(80, 223, 197, 0.055), transparent 42%),
			linear-gradient(145deg, rgba(10, 23, 19, 0.96), rgba(4, 10, 8, 0.99));
		box-shadow: var(--shadow-lift);
	}

	.memory-stage > header {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 0.75rem;
		min-height: 3rem;
		padding: 0.95rem 1rem;
		border-bottom: 1px solid var(--color-line);
		color: var(--color-faint);
		font-family: var(--font-mono);
		font-size: 0.53rem;
		letter-spacing: 0.1em;
		text-transform: uppercase;
	}

	.stage-state {
		display: inline-flex;
		align-items: center;
		gap: 0.4rem;
		color: var(--color-accent-bright);
		white-space: nowrap;
	}

	.stage-state i {
		width: 0.34rem;
		height: 0.34rem;
		border-radius: 50%;
		background: currentColor;
		box-shadow: 0 0 0.8rem currentColor;
	}

	.stage-body { padding: 1.35rem 1.05rem; }

	.message-meta {
		display: flex;
		justify-content: space-between;
		color: var(--color-faint);
		font-size: 0.65rem;
	}

	.message-meta strong { color: var(--color-cream); font-weight: 500; }

	.message-bubble {
		margin-top: 0.9rem;
		padding: 0.95rem;
		border-radius: 0.25rem 0.9rem 0.9rem;
		background: rgba(21, 65, 52, 0.58);
		color: var(--color-muted);
		font-size: 0.73rem;
		line-height: 1.55;
	}

	.extract-block {
		margin-top: 1.4rem;
		padding-top: 1rem;
		border-top: 1px solid var(--color-line);
	}

	.extract-block > p,
	.no-context {
		color: var(--color-accent-bright);
		font-family: var(--font-mono);
		font-size: 0.52rem;
		letter-spacing: 0.12em;
		text-transform: uppercase;
	}

	.knowledge-tags,
	.memory-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.35rem;
		margin-top: 0.8rem;
		padding: 0;
		list-style: none;
	}

	.knowledge-tags span,
	.memory-actions li {
		padding: 0.38rem 0.5rem;
		border: 1px solid var(--color-line);
		border-radius: 999px;
		color: var(--color-faint);
		font-family: var(--font-mono);
		font-size: 0.48rem;
		letter-spacing: 0.07em;
		text-transform: uppercase;
	}

	.knowledge-tags span:first-child {
		border-color: rgba(80, 223, 197, 0.26);
		color: var(--color-accent-bright);
	}

	.face-stack {
		position: relative;
		min-height: 18.4rem;
		margin: 1rem;
	}

	.memory-face {
		position: absolute;
		width: 74%;
		min-height: 11.6rem;
		padding: 0.95rem;
		border: 1px solid var(--color-line-strong);
		border-radius: 0.95rem;
		background: linear-gradient(145deg, rgba(13, 28, 24, 0.99), rgba(5, 12, 10, 0.99));
		box-shadow: 0 20px 55px rgba(0, 0, 0, 0.34);
	}

	.sql-face { left: 0; top: 0; rotate: -4deg; opacity: 0.72; }
	.graph-face { left: 13%; top: 2.25rem; z-index: 2; border-color: rgba(119, 162, 245, 0.38); }
	.wiki-face { left: 26%; top: 4.75rem; z-index: 3; rotate: 2.5deg; border-color: rgba(239, 120, 216, 0.32); }

	.face-heading {
		display: flex;
		justify-content: space-between;
		gap: 0.75rem;
		padding-bottom: 0.65rem;
		border-bottom: 1px solid var(--color-line);
		font-family: var(--font-mono);
		font-size: 0.52rem;
		letter-spacing: 0.06em;
		text-transform: uppercase;
	}

	.face-heading strong { color: var(--color-cream); }
	.face-heading span { color: var(--color-faint); text-align: right; }

	.sql-face ul { margin: 0.65rem 0 0; padding: 0; list-style: none; }
	.sql-face li { display: flex; justify-content: space-between; margin-top: 0.5rem; color: var(--color-faint); font: 0.5rem/1.2 var(--font-mono); }
	.sql-face b { color: var(--color-muted); font-weight: 500; }

	.graph-face svg { width: 100%; margin-top: 0.55rem; overflow: visible; }
	.graph-face path { fill: none; stroke: rgba(99, 225, 192, 0.55); stroke-width: 1.5; }
	.graph-face circle { fill: var(--color-accent-bright); filter: drop-shadow(0 0 0.35rem rgba(99, 225, 192, 0.75)); }
	.graph-face .graph-centre { fill: var(--color-cream); }
	.graph-face .graph-blue { fill: #77a2f5; }
	.graph-face .graph-pink { fill: #ef78d8; }
	.graph-face .graph-gold { fill: #d8b955; }
	.graph-face text { fill: var(--color-faint); font-family: var(--font-mono); font-size: 7px; letter-spacing: 0.04em; text-transform: uppercase; }

	.wiki-path { margin-top: 0.75rem; color: rgba(239, 120, 216, 0.72); font: 0.46rem/1 var(--font-mono); }
	.wiki-face h3 { margin-top: 0.7rem; color: var(--color-cream); font-size: 0.76rem; }
	.wiki-face h3 + p { margin-top: 0.45rem; color: var(--color-faint); font-size: 0.56rem; line-height: 1.5; }
	.wiki-lines { margin-top: 0.65rem; }
	.wiki-lines i { display: block; width: 86%; height: 0.2rem; margin-top: 0.35rem; border-radius: 99px; background: rgba(239, 120, 216, 0.11); }
	.wiki-lines i:nth-child(2) { width: 68%; }
	.wiki-lines i:nth-child(3) { width: 78%; }

	.recall-stage h3 { margin-top: 1.25rem; color: var(--color-cream); font-size: 1rem; line-height: 1.3; }
	.answer-copy { margin-top: 0.8rem; color: var(--color-muted); font-size: 0.69rem; line-height: 1.55; }

	.memory-source {
		display: flex;
		align-items: center;
		gap: 0.55rem;
		margin-top: 1.35rem;
		padding: 0.7rem;
		border: 1px solid rgba(119, 162, 245, 0.24);
		border-radius: 0.7rem;
		background: rgba(119, 162, 245, 0.05);
		color: rgba(160, 185, 232, 0.85);
		font: 0.51rem/1.45 var(--font-mono);
	}

	.memory-source i {
		flex: 0 0 auto;
		width: 0.38rem;
		height: 0.38rem;
		border-radius: 50%;
		background: #77a2f5;
		box-shadow: 0 0 0.8rem #77a2f5;
	}

	.story-flow {
		position: absolute;
		z-index: 6;
		top: 50%;
		width: 1.7rem;
		height: 1px;
		background: linear-gradient(90deg, transparent, var(--color-accent-bright));
	}

	.story-flow::after {
		position: absolute;
		top: -3px;
		right: 0;
		width: 0.42rem;
		height: 0.42rem;
		border-top: 1px solid var(--color-accent-bright);
		border-right: 1px solid var(--color-accent-bright);
		rotate: 45deg;
		content: "";
	}

	.flow-out { left: 100%; }
	.flow-in { right: 100%; rotate: 180deg; }

	@media (max-width: 1050px) {
		.memory-continuity { grid-template-columns: 1fr; }
		.memory-stage { min-height: 19rem; }
		.faces-stage { min-height: 24rem; }
		.face-stack { width: min(100% - 2rem, 34rem); margin-inline: auto; }
		.story-flow { display: none; }
	}

	@media (max-width: 560px) {
		.memory-stage > header { align-items: flex-start; flex-direction: column; }
		.face-stack { width: auto; min-height: 20rem; margin-inline: 0.65rem; }
		.memory-face { width: 82%; }
		.graph-face { left: 9%; }
		.wiki-face { left: 18%; }
	}

	@media (prefers-reduced-motion: reduce) {
		.capture-stage,
		.sql-face,
		.graph-face,
		.wiki-face,
		.story-flow,
		.recall-stage {
			animation: none !important;
			transform: none;
			opacity: 1;
		}
	}
</style>
